//el orden de los operadores ya sean aritmeticos, logicos, etc. Es especifico

//Aqui por ejemplo buscamos el resultado 16;
let resultado = 8 / 2 * (2 + 2);
console.log(resultado);

//y con un "()" podemos hacer que nos de 1:
resultado = 8 / (2 * (2 + 2));
console.log(resultado);

//Todos estos operadores tienen un orden de ejecucion 
//pero si queremos cambiar su orden para que ciertas cosas se ejecuten primero usamos "()"